# Cloudflare Pages
# Dashboard → Create → Connect GitHub → auaa/daily-gitee-probe
#
# Build settings:
#   Framework preset: Vite
#   Build command: npm run build
#   Build output directory: dist
#   Root directory: /
#
# After deploy, open https://<project>.pages.dev and retest 连通性/读写.

name = "daily-gitee-probe"
compatibility_date = "2026-07-25"
pages_build_output_dir = "dist"
