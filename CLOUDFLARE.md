# Cloudflare Pages（通过 Git 连接时）请只用后台「构建设置」：
#   Build command: npm run build
#   Build output directory: dist
#   Deploy command: 留空（不要填 wrangler deploy）
#
# 不要在本仓库放会触发 `wrangler deploy` 的 Workers 配置。
